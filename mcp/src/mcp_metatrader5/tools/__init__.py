"""MCP tool surface — pydantic schemas live in :mod:`.schemas`.

Phase 5 will add the actual tool functions (``mt5_compile_ea`` etc.) here.
For now this package only contains :mod:`.schemas`, which Phase 5 imports.
"""
